import React from 'react';
import { Typography, Box } from '@mui/material';

const AgGridPage = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">AgGrid Page</Typography>
      <Typography variant="body1">This is the AgGrid page content.</Typography>
    </Box>
  );
};

export default AgGridPage;