import React from 'react';
import { Typography, Box } from '@mui/material';

const Login = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Login Page</Typography>
      <Typography variant="body1">This is the Login page content.</Typography>
    </Box>
  );
};

export default Login;